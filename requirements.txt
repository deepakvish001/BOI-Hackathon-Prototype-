# BODHI MULE HUNTER AI - runtime dependencies
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
xgboost>=2.0
networkx>=3.2
pyarrow>=14.0        # parquet round-trip for the simulated tables
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9
matplotlib>=3.8

# Note: there is deliberately no deep-learning framework here. GraphSAGE and
# the temporal graph network are implemented in NumPy with hand-derived
# gradients (verified in tests/test_gradients.py), so the whole engine installs
# from a handful of wheels and runs on a laptop CPU.

# Dev / test
pytest>=8.0
httpx>=0.27
